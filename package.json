{
  "name": "aloft",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Aloft — a superhero flight game over a golden-hour city, built with Three.js.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test \"tests/*.test.js\""
  },
  "dependencies": {
    "three": "^0.186.1"
  },
  "devDependencies": {
    "vite": "^8.3.1",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
