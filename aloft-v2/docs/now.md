# Now

- **Milestone:** M0 (foundations) complete → next M1 (port the look, city blueprint, building-space facade).
- **Last verified:** `npm run check` green (46 tests), `npm run e2e` green (desktop + phone),
  `npm run godot:check` green (3,040 checks).
- **Next steps (M1):** port atmosphere/sky/ocean/terrain/clouds/post/hero/cape/speed effects/HUD from v1;
  `core/city-blueprint.ts` reproducing v1's generation (2,648 building ids); building-space facade;
  temporary v1 grid collision; v1 audio with looping noise.
